const btn_chat = document.getElementById("btn_chat")

const clickHandler = (e) => {
    // 사용자 조회 api 호출해서 체크
    // 로그인이 안 되어 있으면 로그인 알림창 (/popup)
    
    // 되어 있으면 chat 페이지로 이동
    
}


btn_chat.addEventListener("click", clickHandler)
